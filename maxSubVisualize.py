import numpy as np
from matplotlib import pyplot as plt
from matplotlib import animation



def main():
    print("Hello World!")

    #Begin by displaying the set in set notation

    #Display the current values of max, curr, and spec

    #For each number in the sub
        #Draw a line under the number
            #If the number will be added to the subset, turn it green and
            #increment curr

            #If the number is to be speculatively added, turn it yellow and
            #show the new value of spec

            #If the current spec is now greater than curr, replace max if
            #needed, turn all of the currently highlighted numbers to red and
            #turn the new subset origin to green

    #After checking every number, highlight the greatest subset in purple and
    #show its total value

main()
